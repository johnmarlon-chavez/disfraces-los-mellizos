/**
 * Contraseña de la dueña pedida en el momento para una acción reservada a ella,
 * sin cerrar la sesión de Trabajadores. Se verifica en el proceso main (fase 7).
 */
export interface AutorizacionDuena {
  contrasena: string
}
